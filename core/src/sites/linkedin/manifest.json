{
  "id": "linkedin",
  "name": "LinkedIn",
  "domains": ["linkedin.com", "*.linkedin.com"],
  "notes": ["knowledge.md"],
  "browserTools": {
    "pageState": {
      "description": "Read the current LinkedIn route, authentication gate, challenge, rate-limit, and hydration state.",
      "path": "page_scripts.js",
      "binding": "window.SocaiLinkedInPageScripts",
      "callable": "pageState",
      "args": {},
      "returns": { "type": "object", "description": "Current LinkedIn page state." }
    },
    "searchState": {
      "description": "Verify that LinkedIn reached the requested people, content, or company search route and report its result state.",
      "path": "page_scripts.js",
      "binding": "window.SocaiLinkedInPageScripts",
      "callable": "searchState",
      "args": {
        "query": { "type": "string", "required": false, "description": "Expected search keywords." },
        "result_type": { "type": "string", "required": false, "description": "Expected result type: people, content, companies, or all." }
      },
      "returns": { "type": "object", "description": "Search route and result state." }
    },
    "searchResults": {
      "description": "Collect LinkedIn people, content, or company search results across lazy-scroll pages up to limit, with canonical identity and preview assets.",
      "path": "page_scripts.js",
      "binding": "window.SocaiLinkedInPageScripts",
      "callable": "searchResults",
      "args": {
        "limit": { "type": "integer", "required": false, "description": "Maximum results to return." },
        "result_type": { "type": "string", "required": false, "description": "Filter to people, content, companies, or all." },
        "viewport_only": { "type": "boolean", "required": false, "description": "Only include results intersecting the viewport." }
      },
      "returns": { "type": "array", "description": "Visible LinkedIn search results." }
    },
    "scrollResults": {
      "description": "Scroll a LinkedIn search page by one bounded viewport step to hydrate more results.",
      "path": "page_scripts.js",
      "binding": "window.SocaiLinkedInPageScripts",
      "callable": "scrollResults",
      "args": {
        "nudge_up": { "type": "boolean", "required": false, "description": "Scroll slightly upward instead of down." },
        "to_top": { "type": "boolean", "required": false, "description": "Reset the result page to the top." }
      },
      "returns": { "type": "object", "description": "Search scroll and result-count state." }
    },
    "profileDetail": {
      "description": "Extract visible profile identity, relationship labels, landing-page history, and avatar for research.",
      "path": "page_scripts.js",
      "binding": "window.SocaiLinkedInPageScripts",
      "callable": "profileDetail",
      "args": {},
      "returns": { "type": "object", "description": "Visible LinkedIn profile details or an explicit gate state." }
    },
    "profileHistory": {
      "description": "Extract complete visible work or education entries from a LinkedIn profile details route.",
      "path": "page_scripts.js",
      "binding": "window.SocaiLinkedInPageScripts",
      "callable": "profileHistory",
      "args": {},
      "returns": { "type": "object", "description": "Work or education entries from /in/<id>/details/experience/ or /details/education/." }
    },
    "companyDetail": {
      "description": "Extract the active LinkedIn company landing-page identity, logo, follower count, and employee range.",
      "path": "page_scripts.js",
      "binding": "window.SocaiLinkedInPageScripts",
      "callable": "companyDetail",
      "args": {},
      "returns": { "type": "object", "description": "Visible LinkedIn company details or an explicit route state." }
    },
    "companyPeople": {
      "description": "Extract profiles visibly listed in the company page's People you may know section without claiming they are employees.",
      "path": "page_scripts.js",
      "binding": "window.SocaiLinkedInPageScripts",
      "callable": "companyPeople",
      "args": {
        "limit": { "type": "integer", "required": false, "description": "Maximum visible profile suggestions to return." }
      },
      "returns": { "type": "object", "description": "Explicit route/gate state, associated-member count, and visible profile suggestions with their source section." }
    },
    "relatedPeople": {
      "description": "Extract profiles from explicitly labelled related-people sections on the active profile or company page.",
      "path": "page_scripts.js",
      "binding": "window.SocaiLinkedInPageScripts",
      "callable": "relatedPeople",
      "args": {
        "limit": { "type": "integer", "required": false, "description": "Maximum visible related profiles to return." }
      },
      "returns": { "type": "object", "description": "Explicit route/gate state and visible related profiles with their source-section label." }
    },
    "postDetail": {
      "description": "Extract the active LinkedIn post, author, canonical identity, previewable media assets, and engagement metadata.",
      "path": "page_scripts.js",
      "binding": "window.SocaiLinkedInPageScripts",
      "callable": "postDetail",
      "args": {},
      "returns": { "type": "object", "description": "Visible LinkedIn post details or an explicit gate state." }
    },
    "comments": {
      "description": "Collect comments and expanded replies across the active LinkedIn post up to limit; never likes, replies, or submits anything.",
      "path": "page_scripts.js",
      "binding": "window.SocaiLinkedInPageScripts",
      "callable": "comments",
      "args": {
        "limit": { "type": "integer", "required": false, "description": "Maximum visible comments to return." }
      },
      "returns": { "type": "array", "description": "Accumulated, deduplicated LinkedIn comments." }
    },
    "commentEditorTarget": {
      "description": "Inspect the verified active LinkedIn post and return owned pointer geometry for its visible comment editor; never focuses or types.",
      "path": "page_scripts.js",
      "binding": "window.SocaiLinkedInPageScripts",
      "callable": "commentEditorTarget",
      "args": {
        "post_id": { "type": "string", "required": true, "description": "Expected LinkedIn activity id." }
      },
      "returns": { "type": "object", "description": "Fail-closed comment editor geometry bound to the active post." }
    },
    "commentDraftState": {
      "description": "Read the draft and focus state of the verified active LinkedIn post comment editor.",
      "path": "page_scripts.js",
      "binding": "window.SocaiLinkedInPageScripts",
      "callable": "commentDraftState",
      "args": {
        "post_id": { "type": "string", "required": true, "description": "Expected LinkedIn activity id." }
      },
      "returns": { "type": "object", "description": "Current comment draft state bound to the active post." }
    },
    "commentSubmitTarget": {
      "description": "Return owned pointer geometry for the single enabled comment submit control bound to the verified active LinkedIn post; never clicks it.",
      "path": "page_scripts.js",
      "binding": "window.SocaiLinkedInPageScripts",
      "callable": "commentSubmitTarget",
      "args": {
        "post_id": { "type": "string", "required": true, "description": "Expected LinkedIn activity id." }
      },
      "returns": { "type": "object", "description": "Fail-closed submit geometry bound to the active post." }
    },
    "renderedCommentState": {
      "description": "Count exact rendered comment text inside the verified active LinkedIn post for one-shot reconciliation.",
      "path": "page_scripts.js",
      "binding": "window.SocaiLinkedInPageScripts",
      "callable": "renderedCommentState",
      "args": {
        "post_id": { "type": "string", "required": true, "description": "Expected LinkedIn activity id." },
        "text": { "type": "string", "required": true, "description": "Exact comment text." }
      },
      "returns": { "type": "object", "description": "Exact rendered comment count for reconciliation." }
    },
    "scrollComments": {
      "description": "Load the next bounded batch of LinkedIn comments and collapsed replies using read-only expansion controls.",
      "path": "page_scripts.js",
      "binding": "window.SocaiLinkedInPageScripts",
      "callable": "scrollComments",
      "args": {},
      "returns": { "type": "object", "description": "Comment growth, expansion, and end-of-list state." }
    }
  }
}
